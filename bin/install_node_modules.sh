#!/usr/bin/env bash

# An array of require node modules
required_modules=('bootstrap@v5.3.2' '@popperjs/core')

# Install node modules
function install_node_modules() {
  echo ">>> Install required node_modules:"
  if [ -z "${required_modules}" ]; then
    echo "No node_modules to install"
  else
    for i in ${required_modules[i]}; do
      echo ">>> Installing node_module: ${nm}";
      ( npm install ${i} ; )
    done
  fi
}

# Display completion message
function install_complete(){
  echo "Installation Complete !!!"
}

# Initialize Script
function init() {
  install_node_modules ;
  install_complete ;
}

init